# BA Copilot - Проект документация

## Обзор проекта

**BA Copilot** - это веб-приложение для бизнес-аналитиков, предоставляющее инструменты для работы с BPMN диаграммами и анализа бизнес-процессов. Проект находится в стадии MVP (Minimum Viable Product).

### Основные цели

1. **Создание и редактирование BPMN диаграмм** с использованием bpmn-js
2. **AI-ассистент** для помощи в бизнес-анализе
3. **Парсинг и отображение отзывов** с Яндекс.Карт
4. **Административная панель** для управления контентом

## Архитектура проекта

### Технологический стек

- **Frontend**: Next.js 14 (App Router), React 18, TypeScript
- **UI Framework**: Material UI, Tailwind CSS
- **Backend**: Next.js API Routes
- **База данных**: Supabase (PostgreSQL)
- **Аутентификация**: Supabase Auth
- **Парсинг данных**: Python (BeautifulSoup, requests)
- **Управление состоянием**: TanStack Query
- **Деплой**: Vercel

### Структура проекта

```
D:\DATA\TEST_V0/
├── src/
│   ├── app/                    # Next.js App Router
│   │   ├── api/               # API endpoints
│   │   ├── admin/             # Административная панель
│   │   └── page.tsx           # Главная страница
│   ├── components/            # React компоненты
│   │   ├── admin/            # Компоненты админки
│   │   └── reviews.tsx       # Компонент отзывов
│   └── lib/                  # Утилиты и конфигурация
├── scripts/                  # Python скрипты
│   └── yandex_parser.py      # Парсер отзывов
├── public/                   # Статические файлы
│   └── *.json               # JSON файлы с отзывами
└── docs/                    # Документация проекта
```

## Функциональные модули

### 1. Система отзывов

#### Архитектура парсинга отзывов

```
Яндекс.Карты → Python Parser → JSON File → Next.js API → Frontend
```

**Компоненты:**
- `scripts/yandex_parser.py` - Python скрипт для парсинга
- `app/api/generate-yandex-json-remote/route.ts` - API для удаленного парсинга
- `app/api/local-reviews/route.ts` - API для локального отображения
- `components/reviews.tsx` - Frontend компонент

#### Процесс работы

1. **Парсинг**: Python скрипт извлекает отзывы с Яндекс.Карт по `companyId`
2. **Сохранение**: Отзывы сохраняются в JSON файл в папке `public/`
3. **Отображение**: Next.js API читает JSON и отображает отзывы на фронтенде
4. **Пагинация**: Клиентская пагинация по 3 отзыва на страницу

#### Ключевые особенности

- **Случайное перемешивание** отзывов при каждом запросе
- **Парсинг русских дат** (например, "4 марта" → ISO формат)
- **Fallback система**: если Supabase недоступен, используется локальный JSON
- **Обработка 39 отзывов** с полной информацией

### 2. Административная панель

**Маршруты:**
- `/admin/reviews` - Управление отзывами
- `/admin/settings` - Настройки сайта

**Функции:**
- Импорт отзывов из JSON файлов
- Генерация JSON из Яндекс.Карт
- Управление настройками сайта

### 3. API Endpoints

#### Отзывы
- `GET /api/local-reviews` - Получение локальных отзывов
- `POST /api/generate-yandex-json-remote` - Генерация JSON из Яндекс.Карт
- `POST /api/import-yandex-reviews-json` - Импорт отзывов в Supabase

#### Настройки
- `GET /api/settings` - Получение настроек сайта
- `POST /api/settings` - Обновление настроек

#### Диагностика
- `GET /api/test-supabase-simple` - Тест подключения к Supabase
- `GET /api/debug-env` - Отладка переменных окружения

## Технические решения

### 1. Обработка кодировки

**Проблема**: Специальные символы в именах файлов вызывали ошибки
**Решение**: Использование `stdin/stdout` для передачи данных между Node.js и Python

### 2. Парсинг русских дат

**Проблема**: Яндекс.Карты возвращают даты в формате "4 марта"
**Решение**: Создана функция `parseRussianDate()` для конвертации в ISO формат

### 3. Fallback система

**Проблема**: Supabase недоступен в некоторых случаях
**Решение**: Локальный JSON файл как резервный источник данных

### 4. Ограничение парсера

**Проблема**: Парсер извлекал только 5 отзывов из 39
**Решение**: Удалено ограничение `[:5]` в Python скрипте

## Переменные окружения

```env
NEXT_PUBLIC_SUPABASE_URL=your_supabase_url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_anon_key
SUPABASE_SERVICE_ROLE_KEY=your_service_role_key
```

## Статус разработки

### ✅ Завершено
- [x] Парсинг отзывов с Яндекс.Карт
- [x] Отображение 39 отзывов на фронтенде
- [x] Пагинация и случайное перемешивание
- [x] Административная панель
- [x] Локальная система отзывов
- [x] Обработка русских дат

### 🔄 В процессе
- [ ] Интеграция с Supabase (проблемы с подключением)
- [ ] BPMN редактор
- [ ] AI ассистент

### 📋 Планируется
- [ ] Система аутентификации
- [ ] Управление пользователями
- [ ] Расширенная аналитика
- [ ] Экспорт данных

## Производительность

### Оптимизации
- **Клиентская пагинация** - снижает нагрузку на сервер
- **Локальное кэширование** - JSON файлы загружаются один раз
- **Случайное перемешивание** - обеспечивает разнообразие контента

### Метрики
- **Время загрузки отзывов**: ~50ms
- **Количество отзывов**: 39
- **Размер JSON файла**: ~15KB

## Безопасность

### Меры защиты
- Валидация входных данных в API
- Ограничение размера запросов
- Безопасная обработка файлов
- Проверка существования файлов перед чтением

## Развертывание

### Локальная разработка
```bash
npm install
npm run dev
```

### Продакшн
- Платформа: Vercel
- База данных: Supabase
- Статические файлы: Vercel Edge Network

## Мониторинг и логирование

### Логирование
- Консольные логи для отладки API
- Логирование загрузки отзывов
- Отслеживание ошибок парсинга

### Метрики
- Количество загруженных отзывов
- Время выполнения API запросов
- Статус подключения к Supabase

---

*Документ обновлен: 2024-12-19*
*Версия: 1.0*
