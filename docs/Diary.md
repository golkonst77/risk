# Дневник разработки - BA Copilot

## 2024-12-19

### Наблюдения

#### Проблема с парсером отзывов
**Время**: 14:30  
**Проблема**: Парсер извлекал только 5 отзывов вместо 39 с Яндекс.Карт  
**Анализ**: В файле `scripts/yandex_parser.py` на строке 108 было ограничение `[:5]`, которое ограничивало обработку только первыми 5 отзывами.

**Решение**: Удалено ограничение `[:5]` и заменено на обработку всех найденных отзывов.

**Результат**: ✅ Успешно извлечены все 39 отзывов

#### Проблемы с кодировкой файлов
**Время**: 15:00  
**Проблема**: Специальные символы в именах файлов (например, "Просто Бюро — Яндекс Карты.html") вызывали ошибки `FileNotFoundError` при передаче в Python скрипт.

**Анализ**: Проблема возникала из-за неправильной интерпретации символов при передаче имен файлов между Node.js и Python через командную строку.

**Решение**: Изменена архитектура передачи данных - теперь HTML содержимое читается в Node.js и передается в Python через `stdin`, полностью избегая проблем с именами файлов.

**Результат**: ✅ Проблема решена, импорт работает стабильно

#### Парсинг русских дат
**Время**: 16:00  
**Проблема**: Яндекс.Карты возвращают даты в формате "4 марта", "15 февраля" и т.д., которые не распознаются стандартными методами JavaScript.

**Анализ**: Необходима кастомная функция для парсинга русских дат в ISO формат.

**Решение**: Создана функция `parseRussianDate()` в `app/api/local-reviews/route.ts`:
```typescript
function parseRussianDate(dateStr: string): string {
  const monthMap: { [key: string]: number } = {
    'января': 0, 'февраля': 1, 'марта': 2, 'апреля': 3, 'мая': 4, 'июня': 5,
    'июля': 6, 'августа': 7, 'сентября': 8, 'октября': 9, 'ноября': 10, 'декабря': 11
  }
  // ... логика парсинга
}
```

**Результат**: ✅ Даты корректно отображаются на фронтенде

#### Проблемы с Supabase
**Время**: 17:00  
**Проблема**: Постоянные ошибки `TypeError: fetch failed` при попытках подключения к Supabase, несмотря на правильно настроенные переменные окружения.

**Анализ**: Проблема может быть связана с:
- Сетевыми ограничениями
- Неправильной конфигурацией Supabase
- Проблемами с DNS

**Решение**: Создана fallback система, которая использует локальный JSON файл как резервный источник данных, когда Supabase недоступен.

**Результат**: ✅ Система работает стабильно даже при проблемах с Supabase

### Технические решения

#### Архитектура парсинга отзывов
**Принято решение**: Изменить архитектуру с прямого импорта в базу данных на двухэтапный процесс:
1. Яндекс.Карты → Python Parser → JSON файл
2. JSON файл → Next.js API → Frontend

**Преимущества**:
- Избежание проблем с кодировкой файлов
- Возможность кэширования данных
- Fallback система при проблемах с базой данных
- Простота отладки и тестирования

#### Система пагинации
**Принято решение**: Реализовать клиентскую пагинацию с отображением по 3 отзыва на страницу.

**Особенности**:
- Случайное перемешивание отзывов при каждом запросе
- Локальное пересчитывание агрегатов (средний рейтинг, количество)
- Адаптивный дизайн для мобильных устройств

### Проблемы

#### Сетевые проблемы
**Статус**: 🔄 В процессе решения  
**Описание**: Проблемы с подключением к Supabase в некоторых сетевых окружениях  
**Влияние**: Не критично благодаря fallback системе  
**Планы**: Диагностика и настройка сетевых параметров

#### Отсутствие тестов
**Статус**: ⏳ Не решено  
**Описание**: Проект не имеет автоматических тестов  
**Влияние**: Риск регрессий при изменениях  
**Планы**: Добавить базовые тесты для API и компонентов

### Успехи

#### Полная система отзывов
- ✅ Парсинг 39 отзывов с Яндекс.Карт
- ✅ Отображение с пагинацией
- ✅ Административная панель
- ✅ Fallback система

#### Стабильная работа
- ✅ Система работает без сбоев
- ✅ Быстрая загрузка (~50ms)
- ✅ Корректное отображение всех данных

### Планы на следующий день

1. **Диагностика Supabase**
   - Проверить сетевые настройки
   - Тестировать подключение в разных окружениях
   - Настроить правильную конфигурацию

2. **Добавление тестов**
   - Создать базовые тесты для API
   - Тестирование компонентов
   - Настройка CI/CD

3. **Документация**
   - Обновить техническую документацию
   - Создать руководство по развертыванию
   - Документировать API endpoints

---

## 2024-12-18

### Наблюдения

#### Начало работы над системой отзывов
**Время**: 10:00  
**Задача**: Реализация системы парсинга и отображения отзывов с Яндекс.Карт

**Первоначальный подход**: Прямой импорт отзывов в Supabase из HTML файлов

**Проблемы**: 
- Сложности с кодировкой файлов
- Зависимость от доступности базы данных
- Сложность отладки

**Решение**: Переход на двухэтапную архитектуру с JSON файлами

### Технические решения

#### Выбор технологий
- **Python для парсинга**: BeautifulSoup + requests
- **Next.js API**: Для обработки данных
- **Локальное хранение**: JSON файлы в папке public/
- **Fallback система**: Резервный источник данных

---

*Дневник ведется для обеспечения бесшовной работы различных разработчиков*
*Последнее обновление: 2024-12-19*